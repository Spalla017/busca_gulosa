from vertice import Vertice
from grafo import Grafo

class gulosa:
    def __init__(self, vertice):
        self.vertice = vertice
        self.status = False

    def busca(self, verticeAtual):
        print("Vertice Atual: ", verticeAtual)
        if(verticeAtual == self.vertice or self.distancia.Vertice(verticeAtual)):
            print("Você chegou ao destino!")
        else:
            cr
